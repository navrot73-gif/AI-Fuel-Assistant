{
  "version": 8,
  "name": "ai-fuel-local",
  "sources": {},
  "layers": [
    {
      "id": "bg",
      "type": "background",
      "paint": {
        "background-color": "#E8EAEC"
      }
    }
  ],
  "metadata": {
    "comment": "Minimal offline-first style. Tile sources (OSM raster / OpenFreeMap / VersaTiles) are attached at runtime by MapLibreView.attachTileSourceToStyle(). This style only provides the base background so that markers + route render immediately even before tiles load. See P0-3 fix in PR fix/maplibre-marker-lifecycle-p0."
  }
}
