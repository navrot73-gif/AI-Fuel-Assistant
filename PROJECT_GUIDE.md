# 🚗 AI Fuel Assistant — руководство по проекту

> Обновлено: 04.08.2026. Этот файл — живой контекст проекта.
> В начале нового чата с ИИ отправь ему раздел «🤖 Промпт для восстановления контекста» — и работа продолжится без раскачки.

## 📱 О проекте
Android-приложение для поиска АЗС, сравнения цен и AI-аналитики расхода топлива.
Пользователь видит карту с АЗС, получает AI-подбор лучшей станции, строит маршрут,
ведёт гараж (авто, заправки, ТО) и получает AI-рекомендации по расходу.

## 🛠 Стек
- Kotlin, Jetpack Compose, Material 3 (тёмная тема поддерживается)
- MVVM + StateFlow, Hilt (DI), Room
- Карты: OSMDroid; геолокация: FusedLocationProvider
- AI-роутер с fallback: DeepSeek → HuggingFace (Qwen) → GigaChat → YandexGPT
- Сеть: OkHttp (единый клиент через DI, таймауты 30 с; для роутинга 8 с)
- WorkManager пока НЕ подключён; фоновое обновление актуальных данных АЗС — следующий этап

## 🗂 Структура
- `ai/` — AI-провайдеры, AiRouter (fallback), FuelAnalysisPromptBuilder
- `data/` — GasStationRepository (assets/stations.json), Vehicle/FuelRecord репозитории, Room
- `features/dashboard/` — экран AI-телеметрии расхода
- `ui/map/` — карта, список АЗС, карточка АЗС, маршрут
- `ui/fuel/` — MapViewModel (станции, маршрут, геопозиция)
- `ui/vehicles/`, `ui/components/` — гараж и карточка авто (CarSilhouette)
- `geo/` — NominatimGeocodingProvider, OpenRouteServiceProvider (RoutingProvider)
- `di/` — AppModule (Room, репозитории, OkHttpClient, AiRouter)

## ✅ Что работает (текущее состояние)

### Карта и АЗС
- Маркеры АЗС с цветовой кодировкой (нет топлива / очередь / ок)
- Список «АЗС рядом»: фильтры по топливу, сортировки (Лучшее/Дешевле/Ближе/Без очереди)
- Карточка «✨ AI-подбор» лучшей станции на главном экране
- Карточка АЗС: топливо с ценами, очередь, надёжность; раскрывается при выборе из списка
  (список при этом сворачивается), карточка прокручивается
- 🧭 **Маршрут**: кнопка «Построить маршрут» всегда активна — мгновенно рисуется линия
  (прямая) с плашкой «расстояние/время», затем в фоне уточняется по дорогам через
  OpenRouteService, если задан ORS_API_KEY; «Сбросить» очищает маршрут;
  при выборе другой АЗС старый маршрут сбрасывается автоматически
- «Открыть в навигаторе» — запасная кнопка (Google Maps)
- Кнопка 📍 (FAB) возвращает камеру к текущей позиции

### Гараж
- Карточка авто: ровный силуэт седана (градиент Teal→Amber, арки колёс, фары, дверной шов)
- Бак/запас хода, пробег, расход, число заправок, график расхода, счётчик ТО, последняя заправка

### AI-экран
- Телеметрия: средний расход (л/100 км), эффективность, ₽/км, график за 30 дней
- AI-анализ по данным заправок (мульти-провайдер с fallback)

### Системное
- Тёмная тема (isSystemInDarkTheme)
- Реальная геопозиция вместо хардкода координат
- Dashboard использует реальные АЗС (GasStationRepository), не демо-данные
- Room: `AppDatabase` версии 3, миграция 2→3 с ForeignKey для `fuel_records.vehicleId`
- Hilt: `AppModule` предоставляет Room, DAO, репозитории, OkHttpClient, Nominatim и AiRouter
- NominatimGeocodingProvider уже присутствует в коде и зарегистрирован через Hilt, но его интеграцию в пользовательский сценарий нужно проверить отдельно

## 🔑 API-ключи (local.properties — НЕ коммитится!)
- DEEPSEEK_API_KEY, HUGGINGFACE_TOKEN
- GIGACHAT_AUTHORIZATION_KEY (или CLIENT_ID + CLIENT_SECRET)
- YANDEX_API_KEY + YANDEX_FOLDER_ID
- ORS_API_KEY (маршруты по дорогам; без ключа работает прямая линия)

## 🧪 Технический аудит main — 04.08.2026

### 1. GitHub Actions / CI — 🟢 исправлено
- Lint, unit tests и assembleDebug пока выполняются с `continue-on-error`, чтобы диагностические отчёты успевали загрузиться.
- Добавлен обязательный финальный quality gate: job завершается ошибкой, если lint, тесты или сборка не прошли.
- APK и диагностические артефакты сохраняются при успешной соответствующей стадии.
- Изменение внесено отдельным коммитом `b357fb76bdccb4e60e3a4166c2107890dfdb9ad7`.

### 2. Room — 🟢 базовая конфигурация корректна, требуется усилить тестирование
- `AppDatabase` — версия 3.
- Миграция 2→3 создаёт ForeignKey `fuel_records.vehicleId → vehicles.id` с `ON DELETE CASCADE`.
- `FuelRecordEntity` также декларирует тот же ForeignKey.
- `room.schemaLocation` настроен через KAPT.
- Следующий шаг: добавить migration test и проверить экспорт схемы в CI.

### 3. Hilt / DI — 🟢 конфигурация присутствует
- `AppModule` установлен в `SingletonComponent`.
- Через DI выдаются Room, DAO, репозитории, единый OkHttpClient, Nominatim и AiRouter.
- Следующий шаг: проверить реальные runtime-инъекции на экранах и в ViewModel тестами/сборкой.

### 4. WorkManager / фоновые обновления — 🔴 фактически ещё не реализованы
- В текущем дереве `main` отдельного `worker/` нет.
- В `app/build.gradle.kts` нет зависимости `androidx.work:work-runtime-ktx`.
- Поэтому запись о готовом `StationUpdateWorker` была преждевременной и исправлена в этом руководстве.
- Следующий шаг: сначала определить источник актуальных данных, затем добавить WorkManager и обновление Room-кэша.

### 5. Unit tests — 🟡 есть базовые тесты, покрытия бизнес-логики недостаточно
- `GasStationRepositoryTest` сейчас проверяет `GeoUtils.calculateDistance`: совпадение точки, симметрию, короткую дистанцию и масштаб на экваторе.
- Не покрыты тестами: фильтрация АЗС, сортировки, расчёт «лучшей» станции, Room migration, AI Router fallback.
- Следующий шаг: вынести scoring/фильтрацию в Android-independent UseCases и покрыть их unit-тестами.

## ⚠️ Важное замечание по текущей логике данных
`GasStationRepository` сейчас загружает данные из `assets/stations.json` и кэширует их только в памяти.
Это не является источником актуальных цен. Для настоящего продукта нужен следующий слой:

`Backend/API → нормализация → Room cache → FuelDispatcher → UI/AI`.

Текущая функция `getBestStations()` использует смешивание цены, очереди и надёжности в одном числовом выражении.
Это временная эвристика; до подключения реальных данных её следует заменить отдельным `FuelDispatcher`/scoring UseCase с нормализованными весами и тестами.

## 🗺 Следующие задачи

### Этап 1 — стабилизация
- [x] Сделать lint/tests/build обязательными quality gates в CI
- [ ] Добавить Room migration test
- [ ] Добавить unit-тесты scoring/filter/sort
- [ ] Проверить Hilt runtime-инъекции
- [ ] После определения API данных реализовать WorkManager + Room cache

### Этап 2 — геолокация и профиль автомобиля
- [ ] Полностью подключить NominatimGeocodingProvider к сценарию определения города
- [ ] Выбор активного авто на AI-экране (сейчас первое из списка)
- [ ] Проверка радиуса 50 км как бизнес-правила

### Этап 3 — FuelDispatcher
- [ ] Вынести scoring из GasStationRepository в отдельный UseCase
- [ ] Учитывать цену, расстояние, очередь, наличие, маршрут и свежесть данных
- [ ] Добавить прозрачное объяснение результата для пользователя

### Этап 4 — реальные данные
- [ ] Backend/API актуальных цен вместо assets/stations.json
- [ ] Timestamp и источник каждой цены/наличия
- [ ] Оценка достоверности источника
- [ ] Отзывы и рейтинги АЗС, фото-пруфы цен

### Этап 5 — AI
- [ ] AI Router «гонка»: параллельный запрос провайдерам, первый валидный ответ побеждает
- [ ] Жёсткие таймауты и отмена проигравших запросов
- [ ] AI анализирует только структурированные данные FuelDispatcher и не является источником цен

### Среднесрочные
- [ ] Push-уведомления о снижении цены по избранным АЗС
- [ ] Офлайн-кэш тайлов карты

### Долгосрочные
- [ ] OBD-II телеметрия расхода автоматически
- [ ] Gamification экономии, социальные функции

## 🧪 Тестирование
- Unit: GeoUtils — уже есть
- Unit: фильтрация/сортировка/scoring АЗС — следующий шаг
- Migration: Room 2→3 — следующий шаг
- AI: fallback и ошибки провайдеров — следующий шаг
- Ручное: сценарии карта → список → карточка → маршрут → гараж → AI

## 🤖 Промпт для восстановления контекста в новом чате
«Привет! Продолжаем проект AI Fuel Assistant (Kotlin, Compose, Hilt, OSMDroid, MVVM).
Контекст лежит в PROJECT_GUIDE.md в корне репозитория — сначала прочитай его.
Текущее состояние: карта с АЗС и маршрутами работает (прямая линия мгновенно +
уточнение по дорогам через OpenRouteService при наличии ORS_API_KEY), гараж и AI-экран
работают, тёмная тема есть. Технический аудит main от 04.08.2026 завершён по пяти узлам:
CI исправлен, Room и Hilt проверены, Worker фактически ещё не реализован, тестовое покрытие
бизнес-логики недостаточно. Следующие цели: migration tests → scoring/use cases →
WorkManager после определения API данных → Nominatim integration → active vehicle →
FuelDispatcher → реальные данные → AI Router race.»
